import {User} from "./controllers/low"

// type Routes = {[key:string]: Routes | Controller}
export const routes = {
    user: new User(),
    // a: {
    //     h: new Friend(),
    //     b: {
    //         c: new User()
    //     }
    // },
    // low: new Low()
}